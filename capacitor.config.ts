import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.infilabs.mfactor',
  appName: 'mfactor-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
